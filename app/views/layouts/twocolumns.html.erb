<!DOCTYPE html>
<html>
	<head>
  		<title>Oficina de Gestion de desempeño</title>
  		    <script type="text/javascript">var base_url='<%= root_url %>';</script>
			<%= stylesheet_link_tag :defaults, 'layout', 'login' %>  		
			<%= javascript_include_tag :defaults, 'best_in_place', :others %>
			<%= yield :javascript %>
			<%= yield :styles %>

  		<%= csrf_meta_tag %>
	</head>
	<body>
		<header>
			<% if user_signed_in? %>
				<%= render :partial => 'layouts/header_context' %>
			<% else %>
				<h1>Performa</h1>
			<% end %>
		</header>
		
		<div class="main-content on-3 columns">
			
			<%= render(:partial => 'shared/main_menu') if user_signed_in? %>
			
			<div class="messages">
				<p id="notice" class="flash centered"><%= notice %></p>
	    	<p id="alert" class="flash centered"><%= alert %></p>
			</div>

      			<div class="column fixed" style="width: 780px; margin-left:20px">
				<%= yield %>		
      </div>
				
			<div class="column fixed" style="width: 230px;float:right">
				<%= yield :contextual_legend %>
			</div>
		</div>
	</body>
</html>
