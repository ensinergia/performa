<!DOCTYPE html>
<html>
	<head>
  		<title>Oficina de Gestion de desempeño</title>
  			<script type="text/javascript">var base_url='<%= root_url %>';</script>
			<%= stylesheet_link_tag :defaults, 'layout', 'login', 'jquerytools','megamenu.css' %>  		
			<%= javascript_include_tag :defaults, 'best_in_place','jquery.tools.min','jqueryvalidations','megamenu.js', :others %>
			<%= yield :javascript %>
			<%= yield :styles %>
			
  		<%= csrf_meta_tag %>
	</head>
	<body>
		<header>
			<% if user_signed_in? %>
				<%= render :partial => 'layouts/header_context' %>
			<% else %>
				<h1>Performa</h1>
			<% end %>
		</header>
		<p id="notice" class="flash centered"><%= notice %></p>
    <p id="alert" class="flash centered"><%= alert %></p>
		<div id="errors">
  <h2>Hemos encontrado algunos errores!</h2>
</div>
		<div class="main-content on-3 columns">
			
			
			<div class="column fixed" style="width: 200px">
				<br/>
			</div>
				
			<div class="column fixed" style="width: 780px">
				<%= yield %>		
			</div>
				
			<div class="column elastic" style="width: 250px">
				
				<%= yield :contextual_legend %>

			</div>

		</div>
	</body>
</html>