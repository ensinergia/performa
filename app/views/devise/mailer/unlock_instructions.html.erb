<p><%= t("views.mailer.unlock.title")+" "+@resource.email %>!</p>

<p><%= t("views.mailer.unlock.content1") %></p>

<p><%= t("views.mailer.unlock.content2") %></p>

<p><%= link_to 'Unlock my account', unlock_url(@resource, :unlock_token => @resource.unlock_token) %></p>
