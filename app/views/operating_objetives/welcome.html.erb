	<%= content_for(:op_menu, &Proc.new { "selected" }) %>

<div class="inside-box max-inner-width">
	<div class="strategic-objectives-img image"></div>
	<div class="text-content">
		<h1><%= t('views.operating_objetives.index.empty.title') %></h1>

		<div class="contextual">
			<%= form_tag contextual_legends_show_path, :remote => true, :id => "contextual-show-action-form" do %>
				<%= hidden_field_tag :route, request.env['PATH_INFO'] %>
			<% end %>
		</div>
	</div>
	<br/>
	<% link_to t('views.operating_objetives.index.empty.controls.start'), new_operating_cycle_url(with_current_subdomain), :class => "button orange start-button" %>
</div>
